/**
 * Created by niefz on 2018/1/8.
 */
import VBreadcrumbItem from './src/breadcrumb-item';

VBreadcrumbItem.install = (Vue) => {
  Vue.component(VBreadcrumbItem.name, VBreadcrumbItem);
};

export default VBreadcrumbItem;
