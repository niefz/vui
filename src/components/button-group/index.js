/**
 * Created by niefz on 2018/1/8.
 */
import VButtonGroup from '@/components/button/src/button-group';

VButtonGroup.install = (Vue) => {
  Vue.component(VButtonGroup.name, VButtonGroup);
};

export default VButtonGroup;
